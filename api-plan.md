# API Plan — IT Hub Space

Список планируемых REST-эндпоинтов. Базовый путь: `/api`.
Все защищённые маршруты требуют заголовка `Authorization: Bearer <JWT>`.

---

## Аутентификация

| Метод | URL | Доступ | Описание |
|-------|-----|--------|----------|
| POST | `/api/auth/register` | Публичный | Регистрация нового пользователя |
| POST | `/api/auth/login` | Публичный | Вход; возвращает JWT-токен |

**Тело запроса (login):**
```json
{ "email": "user@ithub.ru", "password": "secret" }
```
**Ответ:**
```json
{ "token": "<JWT>" }
```

---

## Пользователи

| Метод | URL | Доступ | Описание |
|-------|-----|--------|----------|
| GET | `/api/users/me` | JWT | Профиль текущего пользователя |
| PUT | `/api/users/me` | JWT | Обновить имя / email |

---

## Мероприятия

| Метод | URL | Доступ | Описание |
|-------|-----|--------|----------|
| GET | `/api/events` | Публичный | Список всех мероприятий |
| GET | `/api/events/{id}` | Публичный | Детали мероприятия |
| POST | `/api/events` | JWT (ADMIN) | Создать мероприятие |
| PUT | `/api/events/{id}` | JWT (ADMIN) | Обновить мероприятие |
| DELETE | `/api/events/{id}` | JWT (ADMIN) | Удалить мероприятие |

---

## Направления (факультеты)

| Метод | URL | Доступ | Описание |
|-------|-----|--------|----------|
| GET | `/api/faculties` | Публичный | Список специальностей |
| GET | `/api/faculties/{id}` | Публичный | Детали специальности с куратором |

---

## Корпуса

| Метод | URL | Доступ | Описание |
|-------|-----|--------|----------|
| GET | `/api/buildings` | Публичный | Список всех корпусов |
| GET | `/api/buildings/{id}` | Публичный | Детали корпуса |
| GET | `/api/buildings/search?q=` | Публичный | Поиск по названию города или адресу |

---

## Медиа

| Метод | URL | Доступ | Описание |
|-------|-----|--------|----------|
| GET | `/api/media` | Публичный | Все медиафайлы (фото + видео) |
| GET | `/api/media?type=PHOTO` | Публичный | Фильтр по типу: PHOTO / VIDEO |
| POST | `/api/media` | JWT (ADMIN) | Загрузить новый медиафайл |
| DELETE | `/api/media/{id}` | JWT (ADMIN) | Удалить медиафайл |

---

## Портфолио

| Метод | URL | Доступ | Описание |
|-------|-----|--------|----------|
| GET | `/api/portfolios` | Публичный | Студенческие проекты |
| GET | `/api/portfolios/{id}` | Публичный | Детали проекта |
| POST | `/api/portfolios` | JWT | Добавить свой проект |
| DELETE | `/api/portfolios/{id}` | JWT (владелец / ADMIN) | Удалить проект |

---

## Партнёры

| Метод | URL | Доступ | Описание |
|-------|-----|--------|----------|
| GET | `/api/partners` | Публичный | Список компаний-работодателей |
| POST | `/api/partners` | JWT (ADMIN) | Добавить партнёра |

---

## Заявки на поступление

| Метод | URL | Доступ | Описание |
|-------|-----|--------|----------|
| POST | `/api/admissions` | Публичный | Отправить заявку абитуриента |
| GET | `/api/admissions` | JWT (ADMIN) | Просмотреть все заявки |

---

## Коды ответов

| Код | Значение |
|-----|----------|
| 200 | OK |
| 201 | Создано |
| 400 | Ошибка валидации |
| 401 | Не аутентифицирован |
| 403 | Нет прав |
| 404 | Не найдено |
| 500 | Внутренняя ошибка сервера |
